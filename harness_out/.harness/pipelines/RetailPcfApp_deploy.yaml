pipeline:
  name: RetailPcfApp - deploy
  identifier: RetailPcfApp_deploy
  orgIdentifier: my_org
  projectIdentifier: my_project
  tags:
    platform: pcf
    tas: 'true'
  stages:
  - stage:
      name: retail-api
      identifier: retail_api
      type: Deployment
      spec:
        deploymentType: TAS
        service:
          serviceRef: retail_api
        environment:
          environmentRef: <+input>
          infrastructureDefinitions:
          - identifier: <+input>
        execution:
          steps:
          - step:
              name: Deploy
              identifier: Deploy
              type: ShellScript
              spec:
                shell: Bash
                onDelegate: true
                source:
                  type: Inline
                  spec:
                    script: 'echo "TODO: implement deployment for component: retail-api"'
  - stage:
      name: retail-web
      identifier: retail_web
      type: Deployment
      spec:
        deploymentType: TAS
        service:
          serviceRef: retail_web
        environment:
          environmentRef: <+input>
          infrastructureDefinitions:
          - identifier: <+input>
        execution:
          steps:
          - step:
              name: Deploy
              identifier: Deploy
              type: ShellScript
              spec:
                shell: Bash
                onDelegate: true
                source:
                  type: Inline
                  spec:
                    script: 'echo "TODO: implement deployment for component: retail-web"'
